/**
 * Superseded — safe to `git rm`.
 *
 * The case-study catalogue is no longer hand-maintained here. It is derived
 * from the portfolio content at build time by ./case-studies.ts: each study's
 * folder is its slug, `order` in its frontmatter places it, and `aliases`
 * lists the old numbered slugs and reader ids that still redirect to it.
 * Nothing in the engine imports this module any more.
 */
export {};
